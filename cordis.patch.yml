# Insert ego-browser: structured browser-automation tools (30+ ego_*) that
# drive the LOCAL official ego lite app through ctx.subprocess (vendored
# runtime as the no-app fallback engine).
#
# All Config fields are optional — defaults are computed in lib/index.js
# (engineMode auto, sane maxOutputBytes / graceMs, empty defaultSpace).
- insert:
    - id: ego-browser
      name: '@dsh-external/ego-browser'
